[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
# Pure unit tests (rules + bot engine) need no Django/DB. The DB/consumer
# integration checks live in tests/integration/run_*.py (run them directly with
# the venv python — see tests/README.md), so they are NOT auto-collected here.
