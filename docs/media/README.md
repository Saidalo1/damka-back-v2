Put demo GIFs here (referenced from the top-level README):
gameplay.gif, online.gif, bot.gif, spectate.gif

Record at http://localhost:3000 with ScreenToGif (Windows). Keep them short
(5–10s), ~800px wide, so the page loads fast on GitHub.
